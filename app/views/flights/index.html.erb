<%= render partial: 'flight_search_form' %>
<% if search_submitted? && @flight_search_results.nil? %>
  <h1>Sorry no flights on that route and date</h1>
<% elsif search_submitted? && !@flight_search_results.nil? %>
  <%= render partial: 'flight_search_results' %>
<% end %>
  
  